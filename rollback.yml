---
- hosts: webservers
  become: true

  pre_tasks:
    - name: Read previous image id
      ansible.builtin.slurp:
        src: /opt/bulletins/release/previous_image
      register: previous_image_file

    - name: Set deploy image to previous
      ansible.builtin.set_fact:
        deploy_image: "{{ previous_image_file.content | b64decode | trim }}"

  roles:
    - role: docker_deploy
      vars:
        deploy_track_releases: false
